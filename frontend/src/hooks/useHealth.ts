/**
 * Example React Query hooks
 * Add your hooks here as you build the frontend
 *
 * Example:
 *
 * export function useGame(id: string) {
 *   return useQuery({
 *     queryKey: ['game', id],
 *     queryFn: () => wordleApi.getGame(id),
 *   })
 * }
 */
