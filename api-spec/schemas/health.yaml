# Health check schemas
HealthResponse:
  type: object
  properties:
    status:
      type: string
      example: ok
    service:
      type: string
      example: kotlin-wordle-training
  required:
    - status
    - service
